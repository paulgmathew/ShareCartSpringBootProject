server.port=${PORT:8080}
spring.profiles.active=dev

# JWT Configuration
# In production, set APP_JWT_SECRET in the environment.
# HMAC-SHA256 requires at least 32 characters.
app.jwt.secret=998877debckdbckbvbsshdcievfigshbcjfhhiegvkkshieuhruhfbckbvbsshdcievfigshbcjfhhiegvkkshieuhruhf
app.jwt.expiration-ms=86400000

# Invite link base URL — override in production via APP_INVITE_BASE_URL env var
app.invite.base-url=${APP_INVITE_BASE_URL:https://sharecart.app/invite}

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true